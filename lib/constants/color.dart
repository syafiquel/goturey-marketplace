import 'package:flutter/material.dart';

// New Modern Color Palette
const primaryColor = Color(0xFF2196F3); // A calm, friendly Blue
const accentColor = Color(0xFFFFC107); // A complementary accent
const primaryColorHex = 0xFF2196F3;


// Background & Surface Colors
const backgroundLite = Color(0xFFFAF7FB); // A clean, off-white
const backgroundDark = Color(0xFF121212); // Standard dark theme background
const cardsLite = Color(0xFFFFFFFF);
const cardsDark = Color(0xFF1E1E1E);

// Text & Icon Colors
const textPrimaryDark = Color(0xFF212121); // Soft dark grey for text
const textPrimaryLight = Color(0xFFFFFFFF);
const iconColor = Color(0xFF616161);
const greyFontColor = Color(0xFF9E9E9E);


// UI Element Colors
const textBoxLite = Color(0xFFFFFFFF);
const textBoxDark = Color(0xFF424242);
const searchBoxBg = Color(0xFFEEEEEE);
const searchBorderBg = Color(0xFFE0E0E0);
const imageBg = Color(0xFFE0E0E0);
var greyShade = Colors.grey.withOpacity(0.5);
var greyShade2 = Colors.grey.shade200;


// Miscellaneous & Dashboard Colors (can be kept or updated as needed)
const boxBg = Color(0XFFF6F6F5);
var gridBg = const Color(0XFFF9F9F9);
const otherImgsBg = Color(0XFFCBCBCC);
const storeColor = Color(0XFFABACB1);

var primaryOpacity = primaryColor.withOpacity(0.8);

var dashBlue = Colors.blue.withOpacity(0.6);
var dashGrey = Colors.grey.withOpacity(0.6);
var dashRed = Colors.red.withOpacity(0.6);
var dashOrange = Colors.orange.withOpacity(0.6);
var dashPurple = Colors.purple.withOpacity(0.6);
var dashTeal = Colors.teal.withOpacity(0.6);
var dashBrown = Colors.brown.withOpacity(0.6);
var dashPink = Colors.pinkAccent.withOpacity(0.6);
var dashLime = Colors.lime.withOpacity(0.6);
